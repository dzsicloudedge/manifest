apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kaloomupf.fullname" . }}-test-connection"
  labels:
    {{- include "kaloomupf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kaloomupf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
