apiVersion: v1
kind: Service
metadata:
  name: {{ include "kaloomupf.fullname" . }}
  labels:
    {{- include "kaloomupf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kaloomupf.selectorLabels" . | nindent 4 }}
